version: '3.7'
services:
  whiteman:
    build:
      context: app
    volumes:
        - ./log:/app/log
    environment:
      BOT_TOKEN: 
      MC_HOST: 
      MC_PASSWORD: 
      TZ: Asia/Tokyo
    restart: always
